import React, {useEffect} from 'react';
import {Container} from "react-bootstrap";

const ProfileView = () => {

  return (
    <Container>
      ProfileView
    </Container>
  );
};

export default ProfileView;